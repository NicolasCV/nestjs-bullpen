## What does this PR do?

<!-- A short summary, and the issue it closes (e.g. "Closes #12"). -->

## Checklist

- [ ] Tests added or updated, and `npm test` passes
- [ ] `npm run typecheck` passes
- [ ] Stays lightweight (no new runtime deps; UI bundle didn't balloon)
- [ ] Stays scale-safe (paginated, counts don't fetch jobs, bulk/export capped)
- [ ] Docs updated under `docs/` (and the README if user-facing)
- [ ] UI changes include a screenshot
